import React, { Component } from 'react';
import PropTypes from 'prop-types';
import d3 from 'd3';

class RaceMatrix extends Component {
    render(){
        return (
            <p>aho</p>
        )
    }
} 

RaceMatrix.propTypes = {
    matrixData: PropTypes.array
};

export default RaceMatrix;